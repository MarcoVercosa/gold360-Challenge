{
  "name": "consume_update_create_bd",
  "version": "1.0.0",
  "description": "Consumer queue aplication",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prismaConfig": "prisma db pull && prisma generate",
    "devts": "ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/index.ts",
    "buildTs": "npx tsc",
    "prod": "npm install && npm run prismaConfig && npm run buildTs && node build/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.25",
    "prisma": "^3.12.0",
    "ts-node-dev": "^1.1.8",
    "tsconfig-paths": "^3.14.1",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@types/amqplib": "^0.8.2",
    "amqplib": "^0.8.0",
    "dotenv": "^16.0.0",
    "@prisma/client": "^3.12.0"
  }
}