{
  "name": "consumerrequestcancelactive",
  "version": "1.0.0",
  "description": "Consumer route Cancel Active register",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prismaConfig": "prisma db pull && prisma generate",
    "devts": "NODE_ENV=development ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/index.ts",
    "buildTs": "npx tsc",
    "prod": "NODE_ENV=production npm install && npm run prismaConfig && npm run buildTs && node build/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^3.13.0",
    "@types/amqplib": "^0.8.2",
    "@types/node": "^17.0.27",
    "amqplib": "^0.8.0",
    "dotenv": "^16.0.0",
    "fastify": "^3.28.0",
    "prisma": "^3.12.0",
    "ts-node-dev": "^1.1.8",
    "tsconfig-paths": "^3.14.1",
    "typescript": "^4.6.3",
    "winston": "^3.7.2"
  }
}
